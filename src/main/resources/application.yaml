spring:
  application:
    name: ShahbazTrades

  jackson:
    deserialization:
      fail-on-null-for-primitives: false

  main:
    lazy-initialization: true

  threads:
    virtual:
      enabled: true

  jmx:
    enabled: false

  mongodb:
    uri: ${MONGO_URI}

server:
  port: ${PORT:8080}
  jetty:
    threads:
      min: 8
      max: 20
