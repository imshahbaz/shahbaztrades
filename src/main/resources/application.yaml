spring:
  application:
    name: trades

  main:
    web-application-type: servlet

  web:
    resources:
      cache:
        period: 360000000
      chain:
        cache: true

  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

  margin:
    leverage: 4

server:
  port: ${PORT:8080}
  address: 0.0.0.0
  forward-headers-strategy: framework
  servlet:
    session:
      cookie:
        http-only: true
        same-site: lax
    context-path: /
    jsp:
      init-parameters:
        development: true
        checkInterval: 0

features:
  swagger:
    enabled: false

springdoc:
  api-docs:
    enabled: ${features.swagger.enabled}
    path: /v3/api-docs
  swagger-ui:
    enabled: ${features.swagger.enabled}
    path: /swagger-ui.html
    url: /v3/api-docs
  webmvc:
    enabled: ${features.swagger.enabled}
