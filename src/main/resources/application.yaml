spring:
  application:
    name: ShahbazTrades

  main:
    lazy-initialization: true

  threads:
    virtual:
      enabled: true

  jmx:
    enabled: false

  mongodb:
    uri: ${MONGO_URI}
  #redis:
    #host: ${REDIS_HOST}
    #port: ${REDIS_PORT}
    #username: ${REDIS_USER}
    #password: ${REDIS_PASS}
#    ssl:
#      enabled: true


server:
  port: ${PORT:8080}
  jetty:
    threads:
      min: 8
      max: 20
